# Các c?u hình không nh?y c?m ho?c gi? l?i làm m?c ??nh

# Hi?n th? câu l?nh SQL trong log (th??ng dùng ?? debug)
spring.jpa.show-sql=true
spring.jpa.format-sql=true

# Hibernate DDL auto setting (none: không t?o l?i DB schema, phù h?p cho production)
spring.jpa.hibernate.ddl-auto=none

# Hibernate Dialect (có th? gi? m?c ??nh n?u môi tr??ng không thay ??i)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# CORS allowed origins (l?y t? môi tr??ng n?u có, m?c ??nh là http://localhost:3000)
cors.allowedOrigins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

# Các thông tin nh?y c?m nh? datasource URL, username, password, và jwt.secret s? ???c l?y t? bi?n môi tr??ng

# Datasource URL (l?y t? bi?n môi tr??ng ho?c có th? gi? m?c ??nh cho local development)
spring.datasource.url=${SPRING_DATASOURCE_URL}

# Datasource username (l?y t? bi?n môi tr??ng)
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}

# Datasource password (l?y t? bi?n môi tr??ng)
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# Datasource driver class (l?y t? bi?n môi tr??ng ho?c gi? m?c ??nh)
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME}

# JWT secret key (l?y t? bi?n môi tr??ng ?? b?o m?t)
jwt.secret=${JWT_SECRET}